{
  "name": "api",
  "version": "0.1.0",
  "description": "API Gateway",
  "scripts": {
    "start": "wunderctl start --debug",
    "dev": "wunderctl up --debug",
    "1dev": "wunderctl up --debug --wundergraph-dir .",
    "build": "wunderctl generate --debug --wundergraph-dir .",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@wundergraph/sdk": "0.128.0",
    "graphql": "16.6.0"
  },
  "devDependencies": {
    "tsconfig": "workspace:*"
  }
}
