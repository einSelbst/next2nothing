{
  "name": "api",
  "version": "0.1.1",
  "private": true,
  "description": "API Gateway",
  "scripts": {
    "dev": "wunderctl up --debug --wundergraph-dir .",
    "build": "wunderctl generate --debug --wundergraph-dir .",
    "start": "wunderctl up --wundergraph-dir .",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@wundergraph/nextjs": "0.7.4",
    "@wundergraph/sdk": "0.129.0",
    "@wundergraph/swr": "0.9.4",
    "graphql": "16.6.0"
  },
  "devDependencies": {
    "tsconfig": "workspace:*"
  }
}
